<?xml version="1.0" encoding="utf-8"?>
<!-- Plain brand green only — no icon. Flutter SplashScreen starts from the exact
     same green background, so the native→Flutter handoff is invisible. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
